import Searches from './RunningHunts/Searches';
import { Box } from '@mui/material';

const SavedHuntsExt = () => (
  <Box sx={{ display: 'flex', justifyContent: 'center' }}>
    <Searches />
  </Box>
);

export default SavedHuntsExt;
