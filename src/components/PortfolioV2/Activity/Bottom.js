import { Grid } from '@mui/material';

const Bottom = () => {
  return (
    <Grid container spacing={5}>
      <Grid item xs={12}>
        Activity Bottom
      </Grid>
    </Grid>
  );
};

export default Bottom;
